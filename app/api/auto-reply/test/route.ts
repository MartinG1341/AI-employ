import { NextRequest, NextResponse } from "next/server";
import { evaluateAutoReply } from "@/src/lib/auto-reply/service";
export async function POST(request: NextRequest) { try { const body = await request.json() as Record<string, unknown>; if (typeof body.message !== "string" || !body.message.trim()) return NextResponse.json({ error: "message is required." }, { status: 400 }); return NextResponse.json(await evaluateAutoReply({ message: body.message, leadId: typeof body.leadId === "string" ? body.leadId : undefined, messageId: typeof body.messageId === "string" ? body.messageId : undefined, conversationId: typeof body.conversationId === "string" ? body.conversationId : undefined, dryRun: true })); } catch { return NextResponse.json({ error: "Unable to evaluate auto-reply." }, { status: 400 }); } }
