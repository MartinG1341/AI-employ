import { NextResponse } from "next/server";
import { decideReply } from "@/src/lib/reply-decision/service";
export async function POST(request: Request) { try { const body = await request.json() as { message?: unknown; leadId?: unknown; messageId?: unknown; leadCategory?: unknown; conversationContext?: unknown }; if (typeof body.message !== "string" || !body.message.trim()) return NextResponse.json({ error: "message is required." }, { status: 400 }); return NextResponse.json({ decision: await decideReply({ message: body.message, leadId: typeof body.leadId === "string" ? body.leadId : undefined, messageId: typeof body.messageId === "string" ? body.messageId : undefined, leadCategory: typeof body.leadCategory === "string" ? body.leadCategory : undefined, conversationContext: Array.isArray(body.conversationContext) ? body.conversationContext.filter((v): v is string => typeof v === "string").slice(-6) : [] }) }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Unable to decide reply mode." }, { status: 400 }); } }
